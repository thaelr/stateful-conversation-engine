# stateful-conversation-engine
Stateful engine for multi-turn LLM interactions with ingress control, extractor-based scene control, a state engine, persistent scene memory, and runtime prompt orchestration. The system normalizes signals, controls model behavior, handles refusals through retry/fallback, and cleans responses before delivery. 
